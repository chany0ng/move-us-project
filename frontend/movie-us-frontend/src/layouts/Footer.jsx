import styled from "styled-components";

const Footer = () => {
  return <CustomFooter>Footer입니다.</CustomFooter>;
};

const CustomFooter = styled.footer`
  height: 20vh;
  background-color: #1f1f1f;
`;

export default Footer;
